source 'https://mirrors.tuna.tsinghua.edu.cn/rubygems/'  # 指定 gem 的源（默认是 RubyGems 官方源）

gem 'jekyll', '~> 4.0'         # 指定依赖的 gem 及版本，~> 表示兼容版本
gem 'bigdecimal'               # 也可以只写 gem 名称，不指定版本

group :development do          # 可以用 group 分组，区分开发环境和生产环境依赖
  gem 'rubocop'
end

